[phases.setup]
nixPkgs = ["nodejs_24"]
aptPkgs = ["curl", "wget"]

[phases.install]
cmds = [
	"npm install -g pnpm@11.6.0",
	"pnpm i --frozen-lockfile --config.engine-strict=false",
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"
